# Summit Candidate GPX Generation Plan

This stage generates unresolved summit-candidate waypoint GPX files.
It does not assign mountain names.
It preserves source tracks unchanged.
It produces exactly one output GPX per input GPX.
It records lineage in `manifest.json`.
It is separate from later feature-table generation and mountain identity resolution.
